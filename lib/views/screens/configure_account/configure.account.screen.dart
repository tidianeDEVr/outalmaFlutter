import 'package:flutter/material.dart';
import 'package:outalma/views/screens/configure_account/components/body.component.dart';

class ConfigureAccountScreen extends StatelessWidget {
  const ConfigureAccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Body());
  }
}
